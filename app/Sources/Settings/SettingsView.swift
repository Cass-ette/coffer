import SwiftUI

struct SettingsView: View {
    var body: some View {
        Text("Settings (Task 17 will complete)")
            .padding()
            .frame(minWidth: 400, minHeight: 300)
    }
}
